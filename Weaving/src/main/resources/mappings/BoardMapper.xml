<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
						"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="BoardDAO">

	<insert id="insertBoard" parameterType="board">
		INSERT INTO
		BOARD(BOARD_ID, EMP_NO, TITLE, BOARD_CONTENTS)
		VALUES((SELECT
		NVL(MAX(BOARD_ID),0) +1 FROM BOARD),
		#{empNo}, #{title},
		#{boardContents})
	</insert>

	<update id="updateBoard" parameterType="board">
		UPDATE BOARD
		SET
		TITLE = #{title},
		BOARD_CONTENTS = #{boardContents}
		WHERE BOARD_ID = #{boardId}
	</update>

	<delete id="deleteBoard">
		DELETE FROM BOARD 
		WHERE BOARD_ID = #{boardId}
	</delete>

	<select id="selectBoardList" parameterType="board"
		resultType="board">
		SELECT E.EMP_NAME, B.*
		FROM BOARD B, EMPLOYEES E
		WHERE
		B.EMP_NO = E.EMP_NO
	</select>

	<select id="selectBoardOne" parameterType="board"
		resultType="board">
		SELECT E.EMP_NAME, B.*
		FROM BOARD B join EMPLOYEES E on B.emp_no = E.emp_no
		WHERE
		B.board_id = #{boardId}
	</select>

</mapper>





