<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
						"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="BoardDAO">
	
	<insert id="insertBoard" parameterType="board">
		INSERT INTO BOARD(BOARD_ID, EMP_NO, TITLE, BOARD_CONTENTS)
		VALUES((SELECT NVL(MAX(BOARD_ID),0) +1 FROM BOARD),
		#{empNo}, #{title}, #{boardContents})
	</insert>	
 
 	<update id="updateBoard">
 		UPDATE BOARD
 		<set>
 			<if test="title !=null"> TITLE = #{title}, </if>
 			<if test="boardContents !=null"> BOARD_CONTENTS = #{boardContents}, </if>
 			<if test="time !=null"> TIME = #{time}, </if>			
 		</set>
 		WHERE BOARD_ID = #{boardId}
 	</update>

	<delete id="deleteBoard">
		DELETE FROM BOARD WHERE BOARD_ID = #{boardId}
	</delete>

	<select id="selectBoardList" resultType="board">
		SELECT * FROM BOARD
	</select>

	<select id="selecttBoardOne">
		SELECT * FROM BOARD WHERE BOARD_ID = #{boardId}
	</select>
	
</mapper>





